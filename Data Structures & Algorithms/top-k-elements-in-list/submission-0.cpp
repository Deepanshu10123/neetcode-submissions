class Solution {
public:
    vector<int> topKFrequent(vector<int>& nums, int k) {
        sort(nums.size(),nums.begin());
        if(k==0 || nums.size()==0)
        {
            return ;
        }
        
        for(int i = 0 ;i<nums.size(); i++)
        {
            if()
        }
    }
};
